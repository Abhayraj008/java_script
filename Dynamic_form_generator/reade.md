# in this project first we will dynamically create a code in which the quiz will auto generated 

## first we will create a div element which have a class container
#  Then inside the container div tag we will create a another div tag question
# then inside question we will create lable